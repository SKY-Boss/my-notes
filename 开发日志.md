# 随记 App 开发日志

## 项目概况
- 路径：E:\myproject\my_notes
- 框架：Flutter 3.38.7 + Dart 3.10.7
- APK：19.3MB（release + arm64），推送到手机 /sdcard/Download/随记.apk
- 构建命令：`cmd.exe /c "set FLUTTER_STORAGE_BASE_URL=https://storage.googleapis.com&&cd /d E:\myproject\my_notes&&flutter build apk --release --target-platform android-arm64"`

## 核心依赖
sqflite（元数据）、flutter_quill 11.5.0（富文本编辑器）、flutter_slidable 3.1.2（左滑操作）、provider（状态管理）、image_picker、video_player、local_auth、uuid

## 项目结构
```
lib/
├── main.dart              ← 入口：初始化 FileService → DatabaseService
├── app.dart               ← MaterialApp 主题（亮暗色模式）
├── models/
│   ├── note_model.dart     ← 笔记数据模型（含 copyWith 注意 ?? 陷阱）
│   └── folder_model.dart   ← 文件夹模型（含颜色字段）
├── services/
│   ├── database_service.dart  ← SQLite 单例（版本2，含迁移）
│   └── file_service.dart      ← 文件系统管理（MyNotes/{notes.db,contents/,media/,thumbnails/}）
├── providers/
│   └── notes_provider.dart    ← ChangeNotifier 状态管理
├── screens/
│   ├── home_screen.dart       ← 首页 + 左侧抽屉（全部笔记/未分类/文件夹/回收站）
│   ├── editor_screen.dart     ← 富文本编辑器（自建中文工具栏 + 图片全屏查看）
│   ├── trash_screen.dart      ← 回收站（批量选择/还原/删除）
│   └── folders_screen.dart    ← 文件夹管理（备用，已整合到抽屉）
└── widgets/
    └── note_card.dart         ← 笔记卡片（左滑四按钮：置顶/加锁/删除/移动）
```

## 关键决策
1. **数据存储**：SQLite 只存元数据，正文存 Delta JSON 文件，视频图片存 media/ 目录
2. **存储路径**：默认应用私有目录，预留 migrateToPublicStorage() 切换到夸克可备份公共目录
3. **编辑器**：从纯 TextField → flutter_quill，手写 embed builder 渲染图片视频
4. **文件格式**：{"delta": [...], "small": [...]}，兼容旧 Markdown 和纯 Delta 格式
5. **构建优化**：release + arm64 单架构，APK 从 196MB → 19.3MB

## flutter_quill 11.x 踩坑记录
- API 大改：`config`（QuillEditorConfig/QuillSimpleToolbarConfig）非 `configurations`
- Embed builder 需手动实现（ImageEmbedBuilder/VideoEmbedBuilder），不自带
- 格式切换：`Attribute.clone(attr, null)` 是官方取消方式
- 状态追踪：`getSelectionStyle().attributes` 是 `Map<String, Attribute>`，key 是 String，必须用 `.key` 访问
- `formatSelection` 不会自动 toggle，需显式判断当前状态
- `HeaderAttribute` 用 `Attribute.h1/h2/h3` 设置，`const HeaderAttribute()` 取消

## Gradle 构建环境问题
- `FLUTTER_STORAGE_BASE_URL` 环境变量被清华镜像污染，需 `set VAR=` 清空
- Kotlin 增量编译缓存易损坏，`flutter clean` 是常备操作
- Gradle 需在 gradle.properties 配 Clash 代理（127.0.0.1:7897）
- android.newDsl=false 解决 AGP 9 兼容性

## 交互细节 Bug
- "乱码" = 英文未本地化（flutter_quill 默认工具栏 + PopupMenuButton tooltip）
- 文件夹颜色选择器需传弹窗 `setDialogState` 才能刷新（StatefulBuilder）
- `copyWith` 的 `??` 导致 `null` 值被忽略（移出文件夹 bug，改用直接赋值）
- 左滑按钮标签需根据状态切换（"置顶"/"取消置顶"，"加锁"/"解锁"）
- 合并三个媒体按钮为一个，弹出底部菜单选择

## 当前状态
- 基本功能完成：笔记 CRUD、富文本编辑、图片视频插入/全屏查看、文件夹颜色分类、左滑操作、回收站
- 待优化：应用锁、桌面小组件、夸克备份迁移入口
- 微信推送问题：cron deliver 的 weixin 通道与正常网关回复用不同 session
